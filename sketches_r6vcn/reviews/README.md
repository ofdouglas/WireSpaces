# Review outputs

Place independent review files here.

**Naming:** `<archetype_id>_<short_name>_<mapper_agent>_<reviewer_agent>.md`

Use [`../template_review.md`](../template_review.md).

Reviewers must not redesign valid mappings; invalidate only on R6 + overlay violations.
